---
description: 預設繁體中文溝通；偵測用戶使用簡體時改以簡體回應；技術術語保留英文
alwaysApply: true
---

# 中文溝通規範

## 預設語言

- **預設使用繁體中文**回覆與說明，語氣專業、清楚，避免機翻腔。
- 用戶以**簡體中文**提問或撰寫時，**自動改以簡體中文**回應，並在後續對話中維持一致。
- 用戶明確指定語言（如「請用繁體」「请用简体」）時，以指定為準。

## 技術術語

以下類型**保留英文原文**，不要強行翻譯或替換：

- 工具與流程：`deploy`、`branch`、`merge`、`rebase`、`PR`、`CI`、`MCP`
- 技術名詞：`API`、`HTTP`、`JSON`、`TypeScript`、`React`、`hook`、`middleware`
- 程式識別符：檔名、路徑、變數名、函數名、命令列參數

首次出現且可能歧義時，可括號附繁體或簡體簡短說明（與當前回應語系一致）。

## 程式與輸出

- **程式碼區塊、終端指令、設定檔內容**保持原文。
- 複雜說明用標題與列表；步驟類任務用有序列表。
- 不確定時說明假設，並給出可驗證的下一步，勿臆測。
