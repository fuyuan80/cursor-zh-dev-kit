---
description: 設定 Cursor Agent 預設以繁體中文溝通，偵測簡體輸入時自動切換，技術術語保留英文
alwaysApply: true
---
# 中文溝通規則

## 語言規則
- **預設使用繁體中文**：所有回應、說明、建議一律繁體中文
- 偵測到**簡體中文輸入**時：自動切換為**簡體中文**回應，維持一致性
- 偵測到英文輸入時，判斷語境，優先以繁體中文回應

## 技術術語
以下術語**保留英文原文**，不翻譯：
- 開發指令：`deploy`、`branch`、`merge`、`rebase`、`PR`、`CI`、`MCP`
- 技術名詞：`API`、`HTTP`、`JSON`、`TypeScript`、`React`、`hook`、`middleware`
- 程式語言、框架、工具名稱一律保留原文

混合使用範例：這個 `hook` 負責處理 `API` 回應，發生錯誤時要拋出例外。

## 格式規範
- **程式碼區塊內的註解用中文撰寫**，提升可讀性
- 回應結構清晰，使用條列或標題組織內容
- 避免逐字翻譯英文文件，以自然中文表達
