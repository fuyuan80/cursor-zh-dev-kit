---
description: 複雜邏輯、公開 API、TODO/FIXME 使用中文註解與說明
globs: "**/*.{ts,tsx,js,jsx,vue,py,go,rs,java,kt,cs,md}"
alwaysApply: false
---

# 程式碼註解與文件（中文）

在中文專案或團隊慣例下撰寫或修改程式時：

## 必須加中文註解

- **複雜邏輯**：非直觀演算法、狀態機、併發、快取、邊界條件、workaround——說明**意圖與原因**，勿複述程式字面意思。
- **公開 API / 函數**：export 的函數、類別、模組對外介面須有**中文說明**（JSDoc、`///`、`"""` docstring 等，依語言慣例），包含參數、回傳值、可能拋出的錯誤。

## TODO / FIXME

- 使用 `TODO` / `FIXME` 時，**後續描述用中文**，並盡量標註負責人或 issue（若有）：

```text
// TODO: 上線前改為從環境變數讀取 timeout
// FIXME: 併發下可能重複扣款，需加分散式鎖
```

## 命名與混用

- **識別符**（變數、函數、型別、檔名）維持英文，與專案既有風格一致。
- **使用者可見字串**（UI、錯誤提示面向終端用戶）使用與產品一致的繁體或簡體。
- 同一檔案內註解語系與專案一致，勿中英隨意切換。
