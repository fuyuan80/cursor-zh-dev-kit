---
description: 規範程式碼註解使用中文撰寫的時機與格式
alwaysApply: true
---
# 程式碼中文註解規範

## 必須加中文註解的情況
- **複雜邏輯**：非直覺的演算法、巢狀條件、位元運算
- **公開 API / 函式**：參數用途、回傳值、副作用
- **TODO / FIXME**：說明待處理原因與預期做法
- **商業邏輯**：業務規則、特殊限制、例外處理原因

## 不需要加註解的情況
- 變數名稱已清楚表達用途
- 標準 CRUD 操作
- 簡單的 getter/setter

## 格式範例

### 函式說明
```typescript
/**
 * 驗證使用者的付款簽名
 * @param payload - 來自 webhook 的原始內容
 * @param signature - 金流服務提供的 HMAC 簽名
 * @returns 驗證通過回傳 true，失敗拋出 Error
 */
function verifyPaymentSignature(payload: string, signature: string): boolean {
```

### 複雜邏輯
```typescript
// 使用指數退避策略重試，避免短時間內大量請求打垮服務
const delay = Math.min(1000 * 2 ** retryCount, 30000);
```

### TODO
```typescript
// TODO: 目前硬編碼匯率，待串接即時匯率 API（預計 Q3）
const TWD_TO_USD = 0.031;
```
